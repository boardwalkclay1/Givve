# MongoDB connection string (required)
# On Railway: add this as an environment variable in your service settings
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Port (Railway sets this automatically; leave unset locally to use the default 4000)
# PORT=4000
